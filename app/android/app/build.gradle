plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
    id("com.facebook.react")
}

android {
    namespace = "moe.tnxg.push"
    compileSdk = 35

    def releaseStoreFile = System.getenv("TPUSH_SIGNING_STORE_FILE") ?: "${rootProject.projectDir}/../../secure_sign.p12"
    def releaseStorePassword = System.getenv("TPUSH_SIGNING_STORE_PASSWORD")
    def releaseKeyAlias = System.getenv("TPUSH_SIGNING_KEY_ALIAS")
    def releaseKeyPassword = System.getenv("TPUSH_SIGNING_KEY_PASSWORD") ?: releaseStorePassword

    signingConfigs {
        release {
            storeFile = file(releaseStoreFile)
            storeType = "pkcs12"
            storePassword = releaseStorePassword
            keyAlias = releaseKeyAlias
            keyPassword = releaseKeyPassword
        }
    }

    defaultConfig {
        applicationId = "moe.tnxg.push"
        minSdk = 24
        targetSdk = 35
        versionCode = 1
        versionName = "0.1.0"
        buildConfigField("boolean", "IS_NEW_ARCHITECTURE_ENABLED", "false")
        buildConfigField("boolean", "IS_HERMES_ENABLED", "true")
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.release
            minifyEnabled = false
            shrinkResources = false
        }
    }
}

dependencies {
    implementation(project(":tpush-core"))
    implementation("com.facebook.react:react-android")
    implementation("com.facebook.react:hermes-android")
    implementation("androidx.core:core-ktx:1.13.1")
}

react {
    debuggableVariants = []
    autolinkLibrariesWithApp()
}
